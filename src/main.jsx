import React from 'react'
import ReactDOM from 'react-dom/client'
import PixelElevator from './PixelElevator.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PixelElevator />
  </React.StrictMode>,
)